# LdQ-v4

Pour la première fois j’essaie la ré-organisation d’un projet Phoenix pour qu’elle soit vraiment pratique.

J’ai donc résolu le problème des alias qui ne s’ouvraient pas dans VSCode (en faisant des liens symboliques — cf. `do_alias.sh`) et créé le dossier `_LIB_` qui est organisé comme je le veux.

## Déploiement

Jouer (à partir de `ldq/`:

~~~shell
./scripts/deploy.sh
~~~

